spring:
  datasource:
    url: jdbc:h2:mem:bikeshop;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6380
  rabbitmq:
    host: localhost

bikeshop:
  security:
    jwt:
      secret: test-secret-test-secret-test-secret-test-secret
